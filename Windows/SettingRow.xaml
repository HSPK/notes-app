<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="NotesApp.Windows.SettingRow"
    x:Name="ControlRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="Narrow" />
                <VisualState x:Name="Wide">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="760" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="ValueColumn.Width" Value="320" />
                        <Setter Target="ValuePresenter.(Grid.Row)" Value="0" />
                        <Setter Target="ValuePresenter.(Grid.Column)" Value="1" />
                        <Setter Target="ValuePresenter.Margin" Value="0" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Grid
            x:Name="LayoutGrid"
            ColumnSpacing="24"
            RowSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition x:Name="ValueColumn" Width="0" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel VerticalAlignment="Center" Spacing="3">
                <TextBlock
                    FontSize="14"
                    FontWeight="SemiBold"
                    Text="{x:Bind Header, Mode=OneWay}"
                    TextWrapping="Wrap" />
                <TextBlock
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Style="{ThemeResource CaptionTextBlockStyle}"
                    Text="{x:Bind Description, Mode=OneWay}"
                    TextWrapping="Wrap" />
            </StackPanel>

            <ContentPresenter
                x:Name="ValuePresenter"
                Grid.Row="1"
                HorizontalContentAlignment="Stretch"
                VerticalContentAlignment="Center"
                Content="{x:Bind SettingContent, Mode=OneWay}"
                Margin="0,0,0,0" />
        </Grid>
    </Grid>
</UserControl>
