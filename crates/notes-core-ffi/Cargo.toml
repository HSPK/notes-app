[package]
name = "notes-core-ffi"
version = "2.0.0"
edition = "2024"
publish = false

[lib]
crate-type = ["rlib", "staticlib"]

[dependencies]
notes-core = { path = "../notes-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
